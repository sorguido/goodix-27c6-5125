# The private repository versions project evidence, but not ephemeral editor,
# interpreter, test, or coverage output.
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.coverage
*.orig
*.bak
*.tmp
*~

# Private history, evidence, protected material, and superseded artifacts.
# This directory is intentionally preserved only in the private workspace.
red_tag/

# Host-local, hash-verified build inputs. Keep documentation, not RPM payloads.
GoodixArtifacts/opencv-4.13-rpms/*.rpm

GoodixArtifacts/login-header-rpms/*.rpm
