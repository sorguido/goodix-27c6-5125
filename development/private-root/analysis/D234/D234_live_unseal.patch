# D234 unseal diff audit -- no source patch emitted

D234_UNSEAL_DIFF_REVIEW=fail
D234_BLOCKED_BY_UNSEAL_SCOPE_EXPANSION

The smallest possible source edit would only neutralize
`_d233_usb_source_seal()`. That edit would expose individual libusb methods but
would not make a reviewed live path reachable from the shipped entrypoint.

Required additional changes would have to construct and connect at least:

- `SystemOsFacade` and `ProductionOsPreflight` with resolved operator/device paths;
- protected root PSK and target-material loaders;
- `LibusbSystemApi`, `ProductionUsbTransport`, and `ProductionReplayBackend`;
- D234 live schema, counters, checkpoint/final publishers, and terminal mapping;
- the single-shot marker and post-live verification contract.

Those are substantive orchestration and reporting changes, not an unseal-only
patch. Per D234 section 4, no source diff was applied and no hardware path was
executed.
