--- a/src/login-preparation.inc
+++ b/src/login-preparation.inc
@@ -91,7 +91,8 @@
       login_complete (rdev, FALSE, error->message);
       return;
     }
-  if (priv->login_owner)
+  /* Suspend/abandon may have completed the request while open was pending. */
+  if (priv->login_owner && priv->login_invocation && !priv->login_suspend_task)
     g_signal_emit_by_name (dev, "goodix-login-prepare", &started);
   if (!started)
     login_complete (rdev, FALSE, "preparation not started; password required");
