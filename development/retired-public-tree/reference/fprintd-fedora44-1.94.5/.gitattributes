# Preserve upstream bytes; SOURCE_SHA256SUMS verifies the immutable import.
source/** -whitespace
# The upstream README has a seven-character reStructuredText heading underline.
source/README conflict-marker-size=32
